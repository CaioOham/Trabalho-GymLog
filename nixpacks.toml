[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --ignore-scripts --no-optional"]

[phases.build]
cmds = ["mkdir -p dist && echo 'ok' > dist/index.html"]

[start]
cmd = "node server.cjs"
